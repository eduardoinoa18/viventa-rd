/** Next.js config */
/**
 * Note: experimental.appDir is not a valid option in Next 14; App Router is default.
 * Keep config minimal for stability, especially on Windows/OneDrive.
 */
module.exports = {
	reactStrictMode: true,
}